library(readr)
babyNamesUSYOB_full <- read_csv("babyNamesUSYOB-full.csv")
View(babyNamesUSYOB_full)
library(readr)
babyNames <- read_csv("babyNamesUSYOB-full.csv")
View(babyNames)
number.lm <- lm(YearOfBirth ~ Number, data = babyNames)
hist(resid(number.lm))
plot(fitted.values(number.lm), resid(number.lm))
#We can see that the histogram is skewed to the left.
savehistory("~/milestone 8.Rhistory")
